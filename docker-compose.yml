version: '3.8'

services:
  forensic-suite:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mobile_forensic_suite
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always
